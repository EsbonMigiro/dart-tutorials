main() {
  int num = 20 + 20;
  num = num - 3;

// rationship == >= <= !=

  if (num == 0) {
    print(num);
  }
  // unary operator
  num++;
  ++num;
  num += 1;
  num *= 1;
  num = num % 5;

  // logical && and || or

  if (num > 1 && num < 200) {
    print(num);
  }
}
